#include <stdio.h>

int main() {
    int A, B, C, D, resultado;

    scanf("%d %d %d %d", &A, &B, &C, &D);
    
    result = (A * B) - (C * D);
  
    printf("DIFERENCA = %d\n", resultado);

    return 0;
}
